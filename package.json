{
  "name": "konachan-tauri",
  "version": "1.0.9",
  "private": true,
  "type": "module",
  "dependencies": { },
  "scripts": {
    "dev": "pnpm --prefix ./src run dev:tauri",
    "build": "pnpm --prefix ./src run build:tauri",
    "preview": "pnpm --prefix ./src run preview:tauri",
    "tauri": "tauri",
    "prepare": "husky install",
    "postinstall": "cd src && pnpm i"
  },
  "devDependencies": {
    "@commitlint/cli": "^17.6.6",
    "@commitlint/config-conventional": "^17.6.6",
    "husky": "^8.0.3"
  }
}
